option_settings:
  aws:elasticbeanstalk:environment:process:default:
    HealthCheckPath: /api/health
    MatcherHTTPCode: 200
  aws:elasticbeanstalk:application:environment:
    # Ensure we don't hold onto connections too long if this helps, 
    # but primarily this file is for the HealthCheckPath.
    NODE_ENV: production
